# appimage-builder recipe see https://appimage-builder.readthedocs.io for details
version: 1
script:
  # Remove any previous build
  - rm -rf AppDir  | true
  - mkdir -p AppDir/usr
  # Install python3.9
  - cp -r /opt/python3.9/* AppDir/usr/
  - ln -sr AppDir/usr/bin/python3.9 AppDir/usr/bin/python
  # PATCH: remove executable flag from archive files to prevent appimage-builder to search for non-existing ELF flag
  # https://github.com/AppImageCrafters/appimage-builder/blob/2a89cee877a3048852dd02a17a84a1fef7e7b66d/appimagebuilder/builder/runtime/executables_scanner.py#L51
  - for file in $(find AppDir/usr/lib/ -name "*.a"); do chmod -x $file; done
  # Make usr and icons dirs
  - mkdir -p AppDir/usr/src
  - mkdir -p AppDir/usr/share/icons/hicolor/scalable/apps
  # Copy the python application code into the AppDir
  - cp ../main.py ../ui ../lib  AppDir/usr/src -r
  # Add the application's icon
  - cp ../ui/icons/Arch_AWS-Glue_64.svg AppDir/usr/share/icons/hicolor/scalable/apps/awsgluemanager.svg
  # Generate the requirements.txt file
  - python3.9 -m pip install --upgrade pip
  - python3.9 -m pip install pipenv
  - python3.9 -m pipenv lock -r > requirements.txt
  # Install application dependencies in AppDir
  - python3.9 -m pip install --upgrade --isolated --no-input --ignore-installed --prefix=./AppDir/usr wheel
  - python3.9 -m pip install --upgrade --isolated --no-input --ignore-installed --prefix=./AppDir/usr -r ./requirements.txt

AppDir:
  path: ./AppDir

  app_info:
    id: name.giacomofurlan.AWSGlueManager
    name: AWSGlueManager
    icon: awsgluemanager
    version: latest
    # Set the python executable as entry point
    exec: usr/bin/python3.9
    # Set the application main script path as argument. Use '$@' to forward CLI parameters
    exec_args: "$APPDIR/usr/src/main.py $@"

  runtime:
    env:
      # APPDIR_LIBRARY_PATH: $APPDIR/lib
      # APPDIR_LIBRARY_PATH: $APPDIR/lib/x86_64-linux-gnu
      PATH: '${APPDIR}/usr/bin:${PATH}'
      # Set python home
      # See https://docs.python.org/3/using/cmdline.html#envvar-PYTHONHOME
      PYTHONHOME: '${APPDIR}/usr'
      # Path to the site-packages dir or other modules dirs
      # See https://docs.python.org/3/using/cmdline.html#envvar-PYTHONPATH
      PYTHONPATH: '${APPDIR}/usr/lib/python3.9/site-packages'
  files:
    include:
      - /bin/bash
      - /usr/bin/env
      - /bin/sh
      - /lib/x86_64-linux-gnu/libdl.so.2
      - /lib/x86_64-linux-gnu/libz.so.1
      - /lib/x86_64-linux-gnu/libc.so.6
    exclude:
    - usr/share/man
    - usr/share/doc/*/README.*
    - usr/share/doc/*/changelog.*
    - usr/share/doc/*/NEWS.*
    - usr/share/doc/*/TODO.*

  test:
    fedora:
      image: appimagecrafters/tests-env:fedora-30
      command: ./AppRun
      use_host_x: true
    debian:
      image: appimagecrafters/tests-env:debian-stable
      command: ./AppRun
      use_host_x: true
    arch:
      image: appimagecrafters/tests-env:archlinux-latest
      command: ./AppRun
      use_host_x: true
    centos:
      image: appimagecrafters/tests-env:centos-7
      command: ./AppRun
      use_host_x: true
    ubuntu:
      image: appimagecrafters/tests-env:ubuntu-xenial
      command: ./AppRun
      use_host_x: true
AppImage:
  arch: x86_64
  update-information: guess
  sign-key: None
